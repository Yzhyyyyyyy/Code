一、 Part 6 数学 (绝大部分可以放弃)
数学部分在 CSP 前三题中最多只会考到初中/高中基础数学。题单里的高级内容纯属浪费时间。

🟢 必须掌握（用于 T1、T2）：

6.1 位运算： 必须懂 &, |, ^, <<, >>。CSP 经常在状态表示或简单加密中用到。
6.2.1 素数 & 6.2.2 最大公约数 (GCD)： 知道怎么写埃氏筛/线性筛（最基础的即可），会写 gcd 函数（辗转相除法）。
6.6.1 排列组合： 知道 $C_n^m$ 和 $A_n^m$ 怎么算，会用简单的循环求组合数即可。
🔴 坚决不碰（直接跳过，考了也不做）：

6.3 同余方程全家桶： 乘法逆元、中国剩余定理 (CRT) 等。
6.4 博弈论： 考的概率极低。
6.5 概率与期望： 通常是 T4/T5 的压轴题。
6.7 线性代数 & 6.8 多项式： 矩阵加速、高斯消元、FFT 等，纯纯的超纲内容。
6.9 - 6.13 莫比乌斯反演、高级筛法等： 这是省选/国赛级别的内容，看都不要看。
二、 Part 7 数据结构 (只学基础，高级全砍)
数据结构是重灾区。很多同学沉迷于手搓高级树结构，结果 CSP 考场上 T3 的字符串模拟写不出来，直接崩盘。

🟢 必须极其熟练（用于 T2、T3、T4暴力分）：

7.2 栈 & 7.3 队列： 这是 T3 大模拟（比如解析表达式、文件路径处理）的绝对核心！必须熟练使用 STL 的 stack 和 queue。
7.4 并查集 (DSU)： 性价比极高！ 代码只有十几行，但经常能解决 T4 的连通性问题，或者拿到大量部分分。
7.5 二叉堆： 熟练使用 STL 的 priority_queue（优先队列）即可，不需要手写。用于优化 Dijkstra 最短路或贪心题。
🟡 选学（如果前三题已经很稳，想冲 T4 的 40~100 分）：

7.7 树状数组 & 7.8 线段树： 这两个结构专门用来处理“区间修改、区间查询”。CSP 的 T4 几乎必考这个。建议： 只学树状数组（代码短，好背），线段树如果觉得吃力可以直接放弃，用暴力 $O(n^2)$ 拿 T4 的前 40 分。
🔴 坚决不碰（看一眼都是浪费时间）：

7.9 分块、7.10 可并堆。
7.11 主席树、7.12 平衡树 (Splay/Treap)： 代码量巨大，极易写错，考场上写这个纯属找虐。
7.13 - 7.18 树链剖分、树套树、动态树 (LCT)、珂朵莉树等： 这些是专门为防 AK（防满分）设置的，和 300 分的目标毫无关系。
