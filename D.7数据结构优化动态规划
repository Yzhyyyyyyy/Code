// ==================================================================================
//                       数据结构优化 DP (DS Optimized DP) 终极笔记
// ==================================================================================
// 核心哲学：拒绝 O(N) 暴力扫描，使用 O(log N) "高科技" 查找。
// 这里的 DP 不再是"填表格"，而是"一边填表，一边把数据扔进索引里"。

// ==================== 1. 核心定义与两大流派 ====================
// 适用范围：
//   1. 转移方程中存在 "区间查询" 或 "偏序关系" (如 max(dp[j]) where L <= j <= R)。
//   2. 树形 DP 需要支持 "单点修改" 并快速查询全局结果 (动态 DP)。

// 流派一：值域/区间索引流 (Range/Value Optimization)
//   - 对应题目：P3287 [SCOI2014]方伯伯的玉米田, P2605 基站选址, LIS 问题
//   - 核心：把 DP 的值存入 线段树/树状数组。
//   - 口诀：算出一个，插入一个；查的时候，问数据结构要。

// 流派二：动态 DP (Dynamic DP / DDP) —— HLD 的终极形态
//   - 对应题目：P4719 【模板】动态dp
//   - 核心：树剖 + 矩阵乘法。
//   - 口诀：重链用线段树维护矩阵积，轻边作为参数存入矩阵。

// ==================== 2. 模型一：序列上的优化 (LIS 进阶) ====================
// 典型场景：求最长不下降子序列，或者二维偏序 DP。
// 朴素方程：dp[i] = max(dp[j]) + 1, 其中 j < i 且 val[j] <= val[i]
// 痛点：找 max(dp[j]) 需要 O(N)，总共 O(N^2)。
// 优化：建立一个以 "val" 为下标的树状数组/线段树。

#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100005;
int tree[MAXN]; // 树状数组，维护前缀最大值
int a[MAXN], dp[MAXN];
int n;

// 树状数组标准操作
void update(int x, int val) {
    for (; x <= n; x += x & -x) tree[x] = max(tree[x], val);
}
int query(int x) {
    int res = 0;
    for (; x > 0; x -= x & -x) res = max(res, tree[x]);
    return res;
}

void solve_lis_optimized() {
    // 1. 离散化 (如果值域很大，必须先离散化到 1~N)
    // ... discrete(a) ...

    // 2. 边算边存
    for (int i = 1; i <= n; i++) {
        // 核心：直接去数据结构里查 "比我小的数中，dp值最大的是多少"
        int max_prev = query(a[i]); 
        
        dp
