string s2 = s.substr(start_index, length);
//参数 1 (start_index)：从哪个下标开始切（从 0 开始计数）。
//参数 2 (length)：可选。要切多长。如果不写这个参数，默认切到字符串末尾。
reverse可以翻转字符串


“最少链覆盖数 = 最长反链长度”
翻译成人话：最少需要的下降子序列个数 = 原序列的最长上升子序列 (LIS) 的长度。



// cin >> string 会返回流对象，读不到东西时会变 false
    while (cin >> key[rule_count] >> value[rule_count]) {
        rule_count++;
    }




加速cin
ios::sync_with_stdio(false);
    cin.tie(0);





//注意求最小值最大值定义的时候要看取值可以不可以是负数，应该定义为INT_MIN,INT_MAX;





/ 什么时候需要 getchar() 吃回车
// 场景：混合使用“读数值” (cin >>, scanf) 和 “读整行/字符” (getline, getchar, scanf %c) 时。

int n;
string s;
cin >> n;       // 读完数字，光标停在换行符前面
getchar();      // 【关键】吃掉那个换行符，否则 s 会读成空
getline(cin, s);

“输入流指针机制”
翻译成人话：

cin >> a 和 scanf("%d") 很智能：它们会自动跳过空格、Tab、回车，直到找到下一个数据。
getline 和 scanf("%c") 很老实：它们不跳过任何东西。如果你刚输完一个数按了回车，它们会立刻读到那个“回车符”，然后觉得任务结束了（读到了空行/回车）。
// 常用口诀
// 1. 数后读行 (cin >> n 后接 getline)：必须加 getchar()。
// 2. 数后读符 (scanf("%d") 后接 scanf("%c"))：必须加 getchar()。
// 3. 全程读数 (全是 cin >> 或 scanf("%d"))：不需要加，自动跳过。





// 模运算（取余）中的除法陷阱
// 核心原则：取余运算对加减乘都封闭，唯独对除法不封闭！
// 错误写法：(a / b) % p  !=  ((a % p) / (b % p))  <-- 绝对禁止直接除！

// 解决方法 1：数学推导（约分）—— 【首选，最快】
// 翻译成人话：在写代码前，先在草稿纸上把公式里的分母约掉，把除法变成乘法。
// 场景：比如公式是 (k * (k-1)) / k，直接化简成 k-1 再算，别傻傻地先乘再除。

// 解决方法 2：乘法逆元（Modular Inverse）—— 【进阶，万能】
// 翻译成人话：把“除以 b” 变成 “乘以 b 的倒数（逆元）”。
// 公式（费马小定理）：若 p 是质数，则 (a / b) % p  ===  (a * qpow(b, p-2)) % p
// 注意：这需要写快速幂，且要求模数 p 通常为质数。

