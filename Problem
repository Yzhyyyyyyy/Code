一、优先解决：彻底释放被占用的 CLionProjects.exe（最可能）
非系统盘下，“Permission denied” 95% 是 文件被后台进程锁定（之前的运行实例没真正关闭，或崩溃后残留僵尸进程），普通任务管理器可能找不到，用以下方法彻底清理：
操作 1：用命令行强制查找并结束进程（精准高效）
按下 Win+R，输入 cmd，打开「命令提示符」（无需管理员）；
输入以下命令，查找是否有 CLionProjects.exe 进程在运行：
cmd
tasklist | findstr CLionProjects
若输出结果（类似 CLionProjects.exe 1234 Console 1 100,000 K），说明有进程占用，记下前面的 进程号（比如 1234）；
输入以下命令强制结束该进程（替换 1234 为你的进程号）：
cmd
taskkill /f /pid 1234
若有多个结果，重复步骤 4 逐个结束；若提示 “找不到进程”，直接执行下一步。
