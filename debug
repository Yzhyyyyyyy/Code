1.注意数据类型命名的时候不要混淆了，会导致数据丢失
2.逐行阅读代码，小心发生手误
3.发现时间复杂度要优化，第一个考虑二分答案，注意讨论下二分答案的时候judge函数返回的含义，不要把左右变化写反了
第二个可以考虑使用前缀和或者是差分数组。
4.注意函数写明的返回值和实际的返回值要是一样的！
5.注意一个式子定义的数据类型和实际算出来的不是一样的的时候会有数据丢失，警惕int*int，要强转ll
6.使用差分数组的时候注意样确定边界条件的位置，要看清楚可不可以取等，加一减一辨析清楚
