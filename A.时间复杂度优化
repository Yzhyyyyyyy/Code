// 时间复杂度 O(n^2) -> 10万数据必超时
for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
        if (b[j] == a[i]) c[j] = i; 
    }
}
// 时间复杂度 O(n) -> 极快
// 1. 预处理：记录 A 中每个值的位置
for (int i = 0; i < n; i++) pos[a[i]] = i; 

// 2. 转换：直接把 B 的值换成位置
for (int i = 0; i < n; i++) c[i] = pos[b[i]]; 
